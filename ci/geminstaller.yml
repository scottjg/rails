---
gems:
- name: geminstaller
  version: >= 0.4.3
- name: fcgi
  version: >= 0.8.7
# - name: mocha
#   version: > 0.9.3 # 0.9.3 has bugs, need to force a higher version to exist
- name: floehopper-mocha  # use this beta github version until a fixed version > 0.9.3 is released on rubyforge
  version: >= 0.9.3.20081220175348 # 0.9.3 has bugs, need to force a higher version to exist
  install_options: --source=http://gems.github.com --source=http://gems.rubyforge.org
- name: mysql
  #version: >= 2.7
  version: = 2.7
- name: postgres
  version: >= 0.7.9.2008.01.28
- name: rack
  version: '~> 0.4.0'
- name: rake
  version: >= 0.8.1
- name: sqlite-ruby
  version: >= 2.2.3
- name: sqlite3-ruby
  version: >= 1.2.2
