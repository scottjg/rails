box: wercker/ubuntu12.04-ruby2.0.0@0.3.0
build:
    steps:
        - install-packages:
            packages: sqlite3 libsqlite3-dev
        - bundle-install
        - script:
            name: Execute smoke
            code: |
                bundle exec rake smoke
